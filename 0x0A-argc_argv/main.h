#ifndef MAIN_H
#define MAIN_H
#include <limits.h>

int _putchar(char);
int main(int argc, char *argv[]);
int check(char *str);
int convert(char *str);
int convert(int amount, int num, int k);

#endif /*MAIN_H*/
